% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{permutation_transition_test}
\alias{permutation_transition_test}
\title{Permutation Test for Directional Transition Asymmetry}
\usage{
permutation_transition_test(
  data,
  label1,
  label2,
  window_sec = 120,
  n_perm = 1000L,
  label_col = "vocalization_label",
  start_col = "session_relative_start",
  end_col = "session_relative_end",
  session_col = "session_label",
  require_adjacent = TRUE,
  seed = 42L,
  plot = TRUE
)
}
\arguments{
\item{data}{A data frame of vocalization events
(e.g., \code{lys$vocalization_session_map}).}

\item{label1}{Character. First label (the hypothesised trigger).}

\item{label2}{Character. Second label (the hypothesised response).}

\item{window_sec}{Numeric. Matching window passed to
\code{compute_onset_latency()}. Default \code{120}.}

\item{n_perm}{Integer. Number of permutation iterations. Default \code{1000}.}

\item{label_col}{Character. Column holding the event label.
Default \code{"vocalization_label"}.}

\item{start_col}{Character. Column holding event start time.
Default \code{"session_relative_start"}.}

\item{end_col}{Character. Column holding event end time.
Default \code{"session_relative_end"}.}

\item{session_col}{Character. Grouping column for sessions.
Default \code{"session_label"}.}

\item{require_adjacent}{Logical. Passed to \code{compute_onset_latency()}.
Default \code{TRUE}.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.
Default \code{42}.}

\item{plot}{Logical. If \code{TRUE} (default), draw a histogram of the null
distribution with the observed statistic marked.}
}
\value{
Invisibly, a list with:
\describe{
\item{\code{observed}}{Observed asymmetry score (n1 - n2).}
\item{\code{null_distribution}}{Numeric vector of permuted scores.}
\item{\code{p_value}}{One-sided p-value: proportion of permuted scores
\eqn{\geq} observed.}
}
}
\description{
Tests whether \code{label1} -> \code{label2} transitions are more frequent
than \code{label2} -> \code{label1} by comparing the observed directional
asymmetry score (n1 - n2) against a null distribution generated by randomly
shuffling event labels within each session many times.  This controls for
both the base rates of each event type and the temporal structure of the
recording.
}
\examples{
\dontrun{
res <- permutation_transition_test(
  data       = lys$vocalization_session_map,
  label1     = "BeggingCall",
  label2     = "SongBout",
  window_sec = 120,
  n_perm     = 2000
)
res$p_value
}
}
